version: '3.8'

services:
  rag-app:
    build: .
    container_name: medical-rag-saas
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./faiss_index:/app/faiss_index
    restart: always
